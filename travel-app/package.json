{
  "name": "travel-app",
  "version": "1.0.0",
  "scripts": {
    "start": "cd backend && node server.js",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "engines": {
    "node": "20.11.1",
    "npm": "10.8.1"
  }
}