{
  "name": "travel-app",
  "version": "1.0.0",
  "scripts": {
    "start": "node travel-app/backend/server.js",
    "heroku-postbuild": "cd travel-app/client && npm install && npm run build"
  },
  "engines": {
    "node": "18.x"
  }
}